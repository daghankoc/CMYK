class Rybit extends Phaser.GameObjects.Sprite {
    constructor(scene, x, y, colorID) {
      super(scene, x, y, colorID);
      scene.add.existing(this);
      
      
    }
    update() {
        
    }
    reset() {
        
    }
}